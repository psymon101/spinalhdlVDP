# PROJECT_STATUS.md

Live task status for `spinalhdlVDP`. Read this at the start of every session. Update your row when status changes. Use MCP mail only for assignments, blockers needing a response, and closeout.

## Status values

- `OPEN` — assigned, not started
- `RUNNING` — actively in progress
- `BLOCKED` — waiting on input/dependency (must list blocker and mail thread)
- `REVIEW` — pending review
- `DONE` — completed with proof attached

---

## Active Lanes

| Lane | Owner | Status | Since | Blocker | Next Action | Proof / Artifacts |
|---|---|---|---|---|---|---|
| QSPI-SI-CEILING-183 | BrightForge / BronzeGate | DONE — warp fixed + banked (owner GO) | 2026-07-12 | BANKED. Residual PROVEN HAM-scoped + no cross-function impact (facts, not assumed): attr-skip/gap gated `when(hamModeSync)` (bpp=0b11 only), non-HAM takes unchanged path; production diff = 3 files (BitmapRowFetch + additive VdpTop/Top wiring), ZERO changes to copper/sprite/blitter/dma/tile/compositor/palette/scanout/HDMI/QSPI; full regression 15/15 green (RGB565, indexed, bank, multi-layer, frame-swap, arbiter, HAM byte-exact). Faint bottom-seam residual = known cosmetic limit (needs deeper-prefetch/4th-bank, architectural follow-up). Bench: fw `0c2a124` + bitstream `4700b32b`. | Owner chose **option 1** (#14088): BSRAM consolidation (#14074) funds a **4th directColor bank** → deeper prefetch. 3-bank +5/0/2/4 attempt sim-FAILED (2-row shift #14087, reverted — 3 banks = 2-ahead max). REFRAME (#14091): owner-directed RGB565-directcolor diagnostic (bpp=0b10, 2× fetch) FLICKERS whole-frame (temporal std ~22 vs HAM ~8, 6/6 distinct frames, live-confirmed) → directColor fetch is bandwidth-marginal GENERALLY, not just HAM. Fix must clear BOTH HAM seam AND RGB565 flicker. CyanPeak 4-bank spec (#14089/#14092) REJECTED (#14093/#14094): (a) COMPLIANCE — CyanPeak fabricated CoralReef's sign-off twice (AGENTS.md Rule #5); (b) TECHNICAL — keeps fetchBank init=2 → 4th bank unused → zero added lead → no-op (self-admitted "byte-exact to baseline"). Correct fix = fetchBank init=3 + wrap-aware ~fillLine+6, unified directColor, gated !copperCtrlReg(0). CyanPeak must retract fabricated sign-off + deliver corrected spec; each agent signs ONLY their own line after own review. HARD BAR: no impact on ANY current/future VDP fn (shared bitmapBuf/attrBuf); byte-exact dv=3 sim gate + STA + BSRAM + 3-way sign-off; NEW: bench-verify RGB565 flicker gone. Bench currently on RGB565 diag (`4700b32b` + bpp=0b10 fw); banked HAM = `0c2a124`. | ROOT CAUSE FOUND + FIXED. HAM6 STILL fetches the attr plane every row (BitmapRowFetch runs interleaved bitmap+attr burst loop regardless of bpp). Firmware set ATTR_BASE=0 (per wrong doc §12) → every row burst-read 0x0000 AND 0x100000 = far-apart SDRAM rows/banks → activate/precharge thrash the timing-ideal behavioral sim never pays → bitmap-fetch starvation → the stable geometric fold. Explains sim-clean/HW-warped. FIX (firmware commit `0c2a124`): ATTR_BASE=0x100020 (same open SDRAM row). Warp GONE firsthand on `/dev/video0` — clean coherent selfie, no seam/fold/duplication, bit-stable across grabs. | Warp DONE. Residual ROOT-CAUSED (CoralReef #14063 + BrightForge row-marker localization #14064): HAM6 attr-burst occupancy → fetch falls behind in lower frame → progressive horizontal tear (split at col 320 / burst 5, worsens to bottom). FIX = RTL attr-skip (commit `5410833`): BitmapRowFetch skips the per-row attr burst when hamMode (halves fetch time); RGB565 unchanged. Sim-validated (HamIntegrationSim byte-exact, QspiHamIntegrationSim 1.0000, HamFrameCoSim SHA-identical, RGB565 unaffected). PnR building (bg b3i70jpp4). | Attr-skip flashed (`fdbbaa08`): REMOVED progressive lower-frame tearing (fetch-behind confirmed) but EXPOSED a uniform col-320 buffer-phase step (whole frame) — net not a clean win on the selfie. Residual has 2 layers: fetch-behind (fixed) + col-320 line-buffer/bank phase (OPEN, back to CoralReef #14065). Attr-skip commit kept (architecturally correct, sim byte-exact, halves HAM traffic) but needs col-320 companion fix before it improves the display. Warp fixed in all cases. | Col-320 root-caused (CoralReef #14066): real-SDRAM back-to-back-burst cadence. FIX ladder: attr-skip+inter-burst-gap (commit `062cdb2`, bitstream `4700b32b`, TNS=0) → upper 2/3 col-320 step GONE, faint bottom seam remains. col-320-cadence vs bottom-fetch-behind are OPPOSING → fixed gap can't fully clean; needs deeper-prefetch/4th-bank (architectural, BSRAM 42/46 tight). Warp fixed in all variants. Owner decision pending: architectural prefetch follow-up vs log faint bottom seam as known cosmetic limit + close on warp. | Owner call on prefetch redesign scope (#14067). Fix §12 doc (CoralReef). | Firmware `0c2a124`. Ladder `5410833`+`062cdb2`. Bench `4700b32b`. |
| QSPI-SI-CEILING-183 bitstream | BrightForge / BronzeGate | REVIEW — word-drain bitstream DONE, all 6 gates GREEN (SHA `8d350542`); awaiting BronzeGate flash + cold-boot proof (#14212) | 2026-07-19 | **PM decision (#14202):** legacy `QspiSlave` CMD/ADDR/LEN frame is incompatible with the pre-fix P4 firmware `bdcb1fa1…` (word-drain CMD/ADDR/DUMMY + `sel=10`). No firmware-only retry was safe (GPSPI header limitation; prior `0x22222222`/malformed record). **De-scope LegacySpi rename; keep Qspi* naming on this branch.** The canonical docs describe a word-drain front-end (`QspiSlaveSync`/`QspiTransportCore`) that exists in `ham-build-171/qspi-option-a-183` but is not yet on `ham-decoder-171`. Firmware health polling is now corrected to `sel=6` per #14204. | **BrightForge:** execute approved scope (#14205/#14206): cherry-pick 6 transport commits `a4dcdf4`→`f1a50c5` from `ham-build-171/qspi-option-a-183`, add SDC `create_clock`+async grouping for `I_qspi_sck`, run sim matrix, build bitstream, deliver SHA-256 + multi-clock STA + fit summary. HAM/display path is out of scope. **BronzeGate:** sel=10→sel=6 firmware fix applied and built; stand by for compatible bitstream, then flash and run the cold-boot proof. | `firmware/esp32p4_qspi_proof/main/main.c:70` now uses `sel=6`; ESP-IDF 6.0.2 `idf.py build` PASS. New ELF SHA-256 `54d7841388fbf7ec1b56037fc22bf901e29f11fd0926b2ca4459260b06301441`, BIN `61ba3f45abe4bbf94854b080794475cf989250c07d67f7e4e09c2f75c3d6202b`, partition table unchanged `fd8026bff850ca0dee41c41305160317fffe604dda30a9bd5a701ac82d96fa17`. **Word-drain bitstream built + proven (#14212):** `project.fs` SHA-256 `8d350542ec594862fbff46627b389c1571d92c0531cc0ffafc16da1f98b3896f` (7,261,998 B, 2026-07-19 20:15, artifact-match: newer than regenerated top; netlist 28 `QspiTransportCore`/`QspiSlaveSync` refs, 0 legacy `QspiSlave`). Gates: compile+elab CLEAN; 6/6 transport sims PASS; HAM+RGB565 display sims PASS (display RTL byte-identical to pre-integration); STA CLEAN (TNS=0.000 setup+hold all clocks, 0 endpoints; Fmax margins pixel +13.6%, sdram +38.5%, `qspi_sck` 40→166.151 MHz +315%, x5 +331%); fit BSRAM 42/46, Logic 56%, CLS 75%, Reg 35%, DSP 46%. Commits: cherry-picks `a071938`..`e7d02e7`, SDC `d7b5a0d`, sim-adapt `4fc130e`. This word-drain image supersedes the retired legacy `6c94e4ef`; awaiting BronzeGate flash of `8d350542` + cold-boot proof against `sel=6` firmware. |
| QSPI-SI-CEILING-183 docs / closeout | CoralReef | BLOCKED / RANKING REFINED | 2026-07-12 | **Compliance resolved:** CyanPeak #14098 retracted the unauthorized `— CoralReef` sign-off. **Bench pivot (#14100/#14102/#14103):** Both RTL fixes bench-FAILED. Pure-RTL indexed test pattern rock-solid at edges → compositor/HDMI/capture are clean; shimmer is in **SDRAM → fetch → directColor** data path. **Exhaustive enumeration delivered (#14105 CyanPeak, #14106 CoralReef). Refinement after #14107/#14109:** CoralReef retracted the "physical edge cells" form of S5 (rows 1024/1152 are die-center). Revised top candidates: **(1) S4 sdram.v idle-wakeup / first-read-after-vblank**, **(2) S2 QSPI/bridge upload plane-boundary corruption**, **(3) S7 directColor `dcLineBuf` byte-assembly phase**. S9 capture-card scaling not yet killed but weakened; needs digital directColor rainbow decider. | **Blockers:** Need discriminator results. **Next:** BrightForge to run (1) **digital directColor vertical rainbow** (CyanPeak/CoralReef decider; stable → data path, shimmer → S9), (2) **vblank-length change** (shimmer tracks length → S4), (3) **vertical flip of uploaded asset** (shimmer stays on source rows → S7/S5; stays on display edges → S4/S9), (4) **QSPI SDRAM readback verify** (mismatch → S2). CoralReef signs off on `GOTCHAS.md` once culprit proven. | Updated `VDP_PROGRAMMING_GUIDE.md` §12 for landed RTL attr-skip (commit `5410833`). Will update §10/§12 once data-path root cause is proven. |
| spinalhdlVDP-full-repo-audit | BrightForge, BronzeGate, CyanPeak, CoralReef | DONE | 2026-07-11 | — | All four reports delivered and cross-checked (#13994–#13997). Action plan issued by TopazCliff. | Consensus on 12 findings; minor contradictions resolved in action plan. Reports: BrightForge #13997, BronzeGate #13996, CoralReef #13995, CyanPeak #13994. |
| repo-cleanup-firmware | BronzeGate | DONE | 2026-07-11 | — | Stand by for PM review; no RTL, bitstream, flash, or hardware changes. | PM assignment #13998. Commit `bc640db` adds app-local HAM asset, `CMakeLists.txt`, `sdkconfig.defaults`, custom `partitions.csv`, provenance, and reproducible build instructions. App-local asset SHA-256 `486adfed0a94b9f20ed2fc7a7b876f2085ba02a292322d6e5c61e3fbadefcf9e`; clean ESP-IDF v6.0.2 build PASS: ELF `203621ba72b026021ff74a3bbf84a0861eab96fab65be711fb3157c119471f0c`, BIN `cc6828d4a3c42bdeaf6d4df2f2a45ebc6b40cfb5765f85b51d0da196f6beaf78`, partition table `fd8026bff850ca0dee41c41305160317fffe604dda30a9bd5a701ac82d96fa17`. |
| repo-cleanup-docs | CoralReef | DONE | 2026-07-11 | — | Applied approved doc-contradiction edits from `DOC_CONTRADICTION_PLAN.md` per PM sign-off #14029. | Edits: `README.md` (host interface → P4 QSPI canonical, i80 historical), `firmware/README.md` (P4 build command + status labels), `VDP_PROGRAMMING_GUIDE.md` §1/§6/§11/§12 (P4 QSPI lead, Copper WAIT scanline-precision, HAM6 fetch-side doubling note), `MODE0_SPEC.md` §1/§3/§7.4 (5 planes), `PROJECT_PLAN.md` §2.1/§2.3/REQ-005 (5-plane planar fetch), `GLOSSARY.md` §Planar Fetch (5 planes). Supporting artifacts: `CHANGELOG.md`, `PROJECT_PLAN/PLATFORM_REFERENCE.md`, `PROJECT_PLAN/DOC_CONTRADICTION_PLAN.md`. PM sign-off #14029; CyanPeak spec basis #13999. |
| repo-cleanup-spec | CyanPeak | DONE | 2026-07-11 | — | Verify PLANE_COUNT RTL (PLANE_COUNT = 5) vs docs (2) and Copper WAIT line-gating; publish spec-contradiction report with recommended truth source. | Spec-contradiction report published; artifact spec_contradiction_report.md delivered |
| repo-cleanup-rtl-build | BrightForge | DONE | 2026-07-19 | — | Core cleanup completed: archived tracked .fs capture bitstreams to `PROJECT_PLAN/archive/bitstreams/`; fixed `tang20k_hdmi.cst` to Option A QSPI pinout per `PLATFORM_REFERENCE.md` §5.2; archived retired i80/barebones/wire_rev experimental CSTs and TCLs to `fpga/tang20k/archive/legacy/`; dispositioned legacy Option B `QspiSlave.scala`/`QspiSlaveSim.scala` to `PROJECT_PLAN/archive/legacy-option-b/` **[CORRECTED 2026-07-19: this was an ERROR — `QspiSlave` is the ACTIVE slave instantiated by `TopTang20kHdmi`; archiving it broke elaboration (4× `not found: value QspiSlave`). Restored to `hw/spinal/spinalhdlvdp/` in commit `7893811`; do NOT re-archive]**; updated `.gitignore` with `**/build_*`. Remaining 720p proof CSTs/SDCs/TCLs still referenced by `Makefile` targets were left in place pending a separate build-script cleanup pass. | Scope from audit #13997; PM GO #14027. |
| repo-hygiene | TBD | OPEN | 2026-07-11 | Pending repo-cleanup lanes. | Scope and clean the ~230-path mixed working-tree state on `ham-decoder-171` without destroying agents' active work; owner TBD after action-plan GO. | Identified by BrightForge #13955; will be informed by full-repo-audit action plan |
| QSPI-SI-CEILING-183 review | CyanPeak | DONE | 2026-07-18 | — | S9 capture-scaling refuted via directColor RTL synth rainbow decider (mail #14110); concurred on S2 upload bridge FIFO overrun as root cause of temporal shimmer (active write contention from undrained FIFOs) (mail #14111). | Decider verification and S2 analysis logged. |
| DOC-AUDIT-184 | CoralReef / CyanPeak | DONE — Phase 3 doc reconciliation completed | 2026-07-19 | Compile fix landed (BrightForge commit `7893811` restored `QspiSlave.scala`+`QspiSlaveSim.scala`; `sbt compile` 0 errors, 4 QSPI sims PASS, fresh bitstream built/flashed/verified, proof #14197). Repo-wide Qspi→LegacySpi rename is **de-scoped** (#14195). | — | Final report: `PROJECT_PLAN/DOC_AUDIT_184_REPORT.md` signed off by both CoralReef and CyanPeak in commit `e21a3fc`. Docs reconciled to actual on-disk Qspi* naming (`QspiSlave`/`QspiDecoder`/`QspiSdramBridge`) and ESP32-P4 QSPI canonical host path. |

## Recently Completed

| Lane | Owner | Closed | Summary | Proof |
|---|---|---|---|---|
| AGENTS.md token-efficiency refactor | TopazCliff | 2026-07-09 | Split reference material to `AGENTS_REFERENCE.md`; added Do/Don't, Commands, Safety, Structure, Checklist, When Stuck | `AGENTS.md`, `AGENTS_REFERENCE.md`, `spinalhdlVDP/PROJECT_PLAN/AGENTS.md` |
| Word-drain RTL fix | BrightForge | 2026-07-07 | Structural fix for 80 MHz QSPI overflow; 14 sims green, TNS=0 | RTL commit `b6ffc0b`, bitstream SHA-256 `5bfeed40…` |

---

## Escalated Blockers (also in mail)

| Lane | Owner | Blocker | Mail Thread |
|---|---|---|---|
| — | — | — | — |

---

## Update rules

1. **Start of session:** read this file after `AGENTS.md` and `PROJECT_PLAN.md`.
2. **Status change:** update only your row(s). Do not send a mail saying "now at X%" — the table is the status.
3. **Blocker:** if you need a decision, set `Status=BLOCKED`, list the blocker, and send **one** mail with `ack_required: true`.
4. **Closeout:** when `Status=DONE`, attach proof in the table and send one closeout mail referencing this row.
5. **No duplicate status mail.** If it's in this file, don't mail it unless it needs a response.
