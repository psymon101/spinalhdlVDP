# PROJECT_STATUS.md

Live task status for `spinalhdlVDP`. Read this at the start of every session. Update your row when status changes. Use MCP mail only for assignments, blockers needing a response, and closeout.

## Status values

- `OPEN` — assigned, not started
- `RUNNING` — actively in progress
- `BLOCKED` — waiting on input/dependency (must list blocker and mail thread)
- `REVIEW` — pending review
- `DONE` — completed with proof attached

---

## Active Lanes

> **Release v0.2.0 (2026-07-29):** `topazcliff/scaler-rewrite` merged into `main` at `a442707`; all external-review doc-impact items closed (F1–F9 + HDMI-TX). Tag `v0.2.0` points to `134b4d6`. Working tree clean; no active lanes remaining.

> **BronzeGate 4 MHz canonical closeout packet (2026-07-21):** implemented the PM-directed 4 MHz bulk SDRAM upload policy in commit `17bdfd7` (`QSPI_SDRAM_CLOCK_HZ = 4u * 1000u * 1000u`), updated `firmware/esp32p4_checkerboard/README.md`, and documented GOTCHA-035 in `firmware/GOTCHAS.md`. Clean ESP-IDF v6.0.2 rebuild PASS: ELF `264925e16bcfcc73a19558fd703361f32d92d999fc6394f72bfa6cabd67652a7`, BIN `5a227180989c72ac2df1a00f42d054eb2e41dd98cee46cd7ba8ff2593d4ba896`, partition `fd8026bff850ca0dee41c41305160317fffe604dda30a9bd5a701ac82d96fa17`; flash/verify PASS. Explicit SRAM reload of assigned bitstream `aaa0fea2336081dfb2905246555ecc8e31d3c11528149600a56ef207ba86004` PASS. Ten MCU cold-start cycles all passed (`CHECKERBOARD_TEST PASS`), all health values were `raw=0x00000000`, and all basic plus row-200 samples passed; cycle log hashes: `8887e6ed`, `6f33a906`, `c6902684`, `80f4564a`, `58997473`, `784d3b8e`, `d534ee2e`, `0329e131`, `33abcf62`, `3c5eec24`. Final clean-build serial proof SHA `65fe1076e4f0241a2c3f405b0cd9e16256a0040677783d75927dabc14cae7f4d`. RTSP HDMI proof captured three frames showing a clean static checkerboard; frame hashes `e4100c0b`, `2226d31a`, `429e73fa`. A fresh direct `/dev/video0` YUYV capture is also clean and straight across three frames; hashes `79e17c1f`, `a1f2ccda`, `9e43e991` (single-frame retry `6aaae9ac`).

> **BronzeGate clean-room proof update (2026-07-21):** fixed checkerboard write framing in commit `3d40636` so `REG_WRITE`/`SDRAM_WRITE` use zero dummy bits; the initial clean-room run incorrectly used read-style 2-bit dummy framing and raised sticky `malformed=1`. Corrected ESP-IDF v6.0.2 build PASS: ELF `dff41d2fc099388f2330e3a4d157ce641d2c64a5eb00e91b0d26f653379b6e7b`, BIN `4fa2bbd038be7cc090b3f5e435c6420bba5d37cbc9fcab79152baca3b7a32b94`; flash/verify PASS. After explicit SRAM reload of bitstream `aaa0fea2336081dfb2905246555ecc8e31d3c11528149600a56ef207ba86004`, corrected serial proof `072f11b48fb5a800185c668441e232df0194833bf0a3e1b2d205e7ac5de72c0e` passed health (`raw=0`), all basic and row-200 samples, and `CHECKERBOARD_TEST PASS`. Ten MCU reset cycles reproduced upload corruption intermittently: cycles 02/04/07/10 passed; 01/03/05/06/08/09 failed, with health `raw=0` in every cycle. This is 4/10 pass and remains an upload/SI issue, not malformed framing or display fetch; the assigned 4/2 MHz experiment is next.

> **BronzeGate frequency/capture update (2026-07-21):** 4 MHz SDRAM upload: 3/3 reset cycles passed (`74839de3`, `c4dad2ee`, `07f6208a`); 2 MHz: 3/3 passed (`0664c005`, `f3f06954`, `65df3db9`), all with health `raw=0`. Firmware was restored and flashed at the canonical 8 MHz setting; final serial proof `66e5473bc14d2b81832fcfa05949b226625d8218b8ef10e4dc4b1ac362632318` is `CHECKERBOARD_TEST PASS`. HDMI capture `/tmp/checkerboard_8mhz_final.png` shows the checkerboard pattern but remains visibly noisy/partial on `/dev/video0` (SHA `6d57c3e0f7d3768f13085d5a0d4d85b525f9325790488a72ef84b4c523619919`), so it is recorded as capture evidence, not a clean-display signoff.

> **BronzeGate update (2026-07-21):** #14247 firmware corrections are applied in `firmware/esp32p4_qspi_proof/main/main.c`. ESP-IDF v6.0.2 build PASS (ELF `6c2cd3cd6ced2021c742ee1b9342b2129b86d48615ec3f7f877d3fcd903aff24`, BIN `43a4d0394c930022ca0de06936a37ea11690dd791878696625a4beb45e820d8c`); `/dev/ttyACM0` flash/verify PASS; serial proof `P4_QSPI_MAGIC PASS`, health `0x00000000` before/upload/enable, `INDEXED2_PROOF_DONE pass=1`, `P4_QSPI_APP done pass=1`. A temporary `/dev/video0` capture shows bars but stepped/noisy bands remain (SHA `f5b36020597f970e21e41e4f1393aff66caaae99e9d9c0521eda642d2a5b8201`), so the lane remains blocked on BrightForge sel=8 readback bitstream delivery.

> **BronzeGate readback update (2026-07-21):** verified and flashed BrightForge bitstream `aaa0fea2336081dfb2905246555ecc8e31d3c11528149600a56ef207ba86004`, including the explicit SRAM load. Firmware readback probe build passed; upper bitmap rows/color-boundary words matched (`0x100000`, `0x100080`, `0x100100`, `0x100014`, `0x100028`, `0x10003C`), but first mismatch was lower bitmap row `0x106400`: expected `0x00000000`, got `0xFFFFFFFF`; further lower bitmap samples mismatched while attribute samples `0x110000`, `0x110080`, `0x116400` matched `0xE4E4E4E4`. This is evidence of lower bitmap content corruption in the upload/SDRAM path; exact ownership remains to be resolved with BrightForge. Serial run ended `P4_QSPI_APP done pass=0` as required by the failed readback.

> **BronzeGate discriminator + health-selector update (2026-07-21):** corrected `READ_STATUS_SEL_TRANSPORT_HEALTH` from `0x06` to active word-drain `sel=0x0A`, documented the contract, and committed `383b00c`; ESP-IDF v6.0.2 build PASS (ELF `6cff6c27204fa9ce1fb65ce5ecda15a962b9816b16af12353ddb6aca695447d1`, BIN `bc96096d39f8f1c2f931ef02c44f88bee8fce6b42f3e95dec104b09c408de715`, partition `fd8026bff850ca0dee41c41305160317fffe604dda30a9bd5a701ac82d96fa17`), flash/verify PASS. Fetch-disabled readback is now explicit (`BITMAP_CTRL=0x0002`, layer enable `0`) and reports real health: before/upload/after-enable all `raw=0x00000000`, `overflow=0`, `malformed=0`. Repeated identical runs were intermittent: one passed all 15 samples; the preceding and following runs failed in lower row 200 at `0x106414`/`0x106428`/`0x10643C` (first bad expected `0x55555555`, got `0x00000000`; final run `0x10643C` got `0x55555555`). Commit `09dbd41` retains post-enable health collection after readback failure; final failing run was `P4_QSPI_APP done pass=0`. This confirms fetch-disabled mode does not remove the intermittent lower-bitmap upload corruption; no transport health flag is raised.

> **Consolidated 2bpp external review handoff (2026-07-25):** external reviewer reviewed the full team-message, firmware, and Scala bundles and issued a consolidated verdict. Key decisions: (1) do **not** apply `fillLine + 4` to production fetch-line generation; (2) keep 4 MHz as the canonical bulk SDRAM upload clock for the current wiring; (3) the transport-health selector bug is already resolved (`sel=0x0A`, commit `383b00c`); (4) the display-bank advance-without-completion hazard is confirmed architectural debt and must be tested directly; (5) a new continuous-scanout backlog co-sim (`Indexed2bppBacklogCoSim`) is required. The parent `external-review-2bpp-shimmer` lane is closed to new scope; implementation work is split into the sub-lanes below.

| Lane | Owner | Status | Since | Blocker | Next Action | Proof / Artifacts |
|---|---|---|---|---|---|---|
| standalone-diagnostic-build | BrightForge | REVIEW — all acceptance criteria MET (compile+gen+PnR TNS=0 + 10/10 cold-POR full-screen grid); CyanPeak review + PM closeout pending | 2026-07-29 | — | Option A on branch `brightforge/standalone-diagnostic-build`: `diagnosticMode` param → no-host/QSPI/SDRAM native 640×480 1× grid (useHostInit=false, layer0→test-pattern grid6, color-math op=00, **LinestateStore ships all-lines L0-enabled** so grid fills screen). Module `top_tang20k_diagnostic`; `TopTang20kHdmiDiagnosticVerilog` gen + `build_diagnostic.tcl` + Makefile `diagnostic` target. **Production spot-check PASS** (normalized diff vs main = 0; gated → production untouched). **Diagnostic PnR PASS**: bitstream `60b23c77`, TNS=0 all clocks, Fmax 30.052 MHz, BSRAM 37/DSP 12 (no new). **HW proof PASS: 10/10 cold-POR cycles** full-frame grid (rows 480/480, cols 720/720, HDMI locked, frame byte-identical `7803de18`) + cyan canary. Commits 685580e5/c5c799a7/ac90dfbe. ★First build 3e96c1d3 showed SPARSE grid (linestate every-8th) → fixed via all-lines L0 default; ★capture must settle ~4s post-POR (UVC re-lock). | Task `PROJECT_PLAN/TASKS/standalone-diagnostic-build.md`; proof packet `PROJECT_PLAN/proof_packets/standalone-diagnostic-build/` (PASS.txt, manifest, hashes, source/, synthesis/, hardware/, captures/); assignment #14496/#14497. |
| external-review-doc-cleanup-f1-f7-stale-links | CyanPeak | DONE — 2026-07-29 | 2026-07-29 | — | Standalone diagnostic build procedure documented in [DIAGNOSTICS.md](file:///home/itadmin/github/spinalhdlVDP/PROJECT_PLAN/DIAGNOSTICS.md); BasicPatternSource async-read path documented as approved/deferred in [ADR-008](file:///home/itadmin/github/spinalhdlVDP/PROJECT_PLAN/DECISIONS/ADR-008-BASICPATTERNSOURCE-ASYNC-READS.md); stale VOODOO_ADOPTION_PLAN.md link resolved. | Task file `PROJECT_PLAN/TASKS/external-review-doc-cleanup-f1-f7-stale-links.md`; doc-impact F1 and F7 updated; closeout commit 7615718. |
| scaler-rewrite-merge-prep | TopazCliff / BrightForge | DONE — 2026-07-28; regression PASS; **merged to `main`** at `a442707` | 2026-07-27 | — | Compile/elaboration PASS; all four production-path co-sims PASS; Gowin PnR PASS (TNS=0, setup/hold violated endpoints=0). Bitstream `8b241328…`; generated RTL `7ad5cee1…`. Merge conflicts resolved in `AGENTS.md`, `TopTang20kHdmi.scala`, `VdpTop.scala`. Post-merge sanity `sbt compile` + `Indexed2bppFineCoSim` PASS. Residual F1/F7 doc items and stale `PROJECT_PLAN.md` `VOODOO_ADOPTION_PLAN.md` link remain for follow-up. | Task file `PROJECT_PLAN/TASKS/scaler-rewrite-merge-prep.md`; proof packet `PROJECT_PLAN/proof_packets/scaler-rewrite-merge-prep/`; merge commit `a442707`. |
| quick-cleanup-ignored-artifacts | TopazCliff | DONE — 2026-07-27 | 2026-07-27 | — | Removed safe ignored local artifacts: `.aider.*`, `.metals/`, `.claude/settings.local.json` (+ empty `.claude/`), `.mcp.json`, `.venv-sessionlog/`. Skipped `.worktrees/` because it contains active git worktrees (`native-640-bitmap-148`, `native-640-firmware`). `git status` clean after commit. | Task file `PROJECT_PLAN/TASKS/quick-cleanup-ignored-artifacts.md`; cleanup commit `75d790c`. |
| PROJECT-SYSTEM-MIGRATION-001 | TopazCliff | DONE — Phase 14 closed; observation satisfied; modular system active | 2026-07-27 | — | Controlled conversion to modular documentation/specification/proof system. Preserves `STATUS.md` as live authority; adds shared specs, runbooks, test plans, proof packets, ADRs, and canonical adapter directories. Phase 10 pilot (`2bpp-bank-completion-rtl`) DONE — sim+PnR proof, CyanPeak arch/interface review (#14375) and CoralReef proof-packet/runbook review (#14376) PASS. `2bpp-hardware-reproof-4mhz` DONE — exact approved 4 MHz artifacts flashed, separated serial/readback/health/YUYV proof PASS (#14415). Observation lane `external-review-tierB-measure` DONE — proof packet `5128ff4`, CyanPeak concurrence (#14427), no RTL change. Cutover approved in `PROJECT_PLAN/DECISIONS/ADR-007-MIGRATION-SYSTEM-CUTOVER.md`; superseded files listed in `PROJECT_PLAN/SUPERSEDED.md`; memory `cc2e32ab…`. | Task file `PROJECT_PLAN/TASKS/PROJECT-SYSTEM-MIGRATION-001.md`; ADR `PROJECT_PLAN/DECISIONS/ADR-007-MIGRATION-SYSTEM-CUTOVER.md`; superseded index `PROJECT_PLAN/SUPERSEDED.md`; closeout proof packet `PROJECT_PLAN/proof_packets/PROJECT-SYSTEM-MIGRATION-001/`; pre-migration commit `958a01d`; pilot proof packet `PROJECT_PLAN/proof_packets/2bpp-bank-completion-rtl/`; reproof proof packet `PROJECT_PLAN/proof_packets/2bpp-hardware-reproof-4mhz/`; observation proof packet `PROJECT_PLAN/proof_packets/external-review-tierB-measure/`; active branch `topazcliff/migration-phase11`. |
| EDUCATION-docs-reconcile | CyanPeak | DONE | 2026-07-25 | — | PM-assigned (#14312): reconcile all `EDUCATION/` docs to their indexed topics in `EDUCATION/00_INDEX.md`, create the four TODO docs, and update the index. | Commit `b5e7dd5`. Updated `01_TASKS_1-7_WALKTHROUGH.md`, `07_TASKS_8-10_WALKTHROUGH.md`, `06_DEEP_DIVE_HARDWARE.md` for consistency; created `02_SPINALHDL_PRIMER.md`, `03_VIDEO_TIMING.md`, `04_TILE_GRAPHICS.md`, `05_FPGA_ARCHITECTURE.md`; `00_INDEX.md` statuses all Complete and last-updated date set to 2026-07-25. |
| external-review-2bpp-shimmer | BrightForge / CyanPeak | DONE — consolidated handoff received 2026-07-25; verdict: no `fillLine + 4`, keep 4 MHz, build backlog co-sim, harden bank completion | 2026-07-25 | — | Consolidated external review of all team messages + firmware + Scala. Decisions: do not change production fetch-line arithmetic; keep 4 MHz bulk-upload canonical; treat bank completion as open RTL hardening; build `Indexed2bppBacklogCoSim`. Action items split into sub-lanes. | Consolidated handoff 2026-07-25; `kb/reviews/spinalhdlvdp_all_team_messages_2026-07-25.md`, `spinalhdlvdp_all_firmware_2026-07-25.md`, `spinalhdlvdp_all_scala_2026-07-25.md`. |
| external-docs-system-review | CoralReef / CyanPeak / BrightForge | DONE — PM disposition 2026-07-26 | 2026-07-26 | — | **STATUS.md remains the live authority.** Selective merge applied: ADRs, templates, runbook/test-plan structure, and canonical adapter-directory policy merged into `AGENTS.md`, `PROJECT_PLAN/`, `docs/`, and `kb/`. The 140-file external tree is kept as a reference snapshot under `docs/external_documentation_system/` (header note added to its `README.md`). No canonical adoption of `ACTIVE_LANE.md` / `MASTER_EXECUTION_PLAN.md`. | `PROJECT_PLAN/external_docs_system_review.md`; selective-merge artifacts in `AGENTS.md`, `PROJECT_PLAN/TASKS/PROJECT-SYSTEM-MIGRATION-001.md`, `docs/fpga/BITMAP_ENGINE.md`, `docs/firmware/HOST_TRANSPORT_ABI.md`, `docs/testing/TP-2bpp-backlog-cosim.md`, `docs/runbooks/COSIM_VALIDATION.md`, `kb/TEMPLATE_ADAPTER/`; reference snapshot `docs/external_documentation_system/`. |
| agent-rule-alignment | TopazCliff | DONE | 2026-07-26 | — | Updated `AGENTS.md` and all `AGENTS/*.md` with proof-packet, ADR, runbook, adapter-directory, and interface-checkpoint rules; removed stale `AGENTS/CoralReef_resume.md`. CyanPeak rule/spec consistency review PASS (#14343/#14341/#14340); CoralReef documentation-authority review PASS via `external-docs-system-review` (#14350). | `AGENTS.md`; `AGENTS/{BrightForge,BronzeGate,CyanPeak,CoralReef,TopazCliff}.md`; mail approvals #14343, #14341, #14340, #14350. |
| 2bpp-backlog-cosim | BrightForge | DONE — `5efe049`; sim PASS (nominal clean + forced-late fires) | 2026-07-26 | — | Build `Indexed2bppBacklogCoSim` using production `VdpTop.bitmapSdramFetchLine` and production bank-rotation logic. Continuous free-running pixel + SDRAM clocks, real clock ratio, finite SDRAM latency, refresh stalls, grant accumulation, CDC drain latency, concurrent display-bank consumption. Scoreboard tracks `bank_valid`, `bank_row_tag`, `bank_bitmap_complete`, `bank_attr_complete`, expected display source row, `grantOverflow`, display-underflow, incomplete-bank, wrong-row-tag counts. Closeout must report measured `bestDv` (enforce `bestDv == 3`), max fetch duration, source-row budget, and all counter values. Nominal realistic latency/refresh must show zero violations. Forced-late mode must trigger incomplete-bank or underflow detector with the current design and must fail before `bankReady`/row-tag hardening, then pass afterward. Bank completion is acknowledged only after final bitmap and attribute writes land in the pixel-domain line-buffer bank. | Commit `5efe049` (adds `Indexed2bppBacklogCoSim.scala`; additive sim — no RTL touched, zero regression surface). Uses production `VdpTop.bitmapSdramFetchLine` + 3-bank rotation via `Indexed2bppFrameCoSim.Dut`; `grantOverflow` read from `fetch.sd.grantOverflow`. `sbt runMain spinalhdlvdp.Indexed2bppBacklogCoSim` PASS: **NOMINAL** bestDv=3, wrong=1/480, grantOverflow=0, displayUnderflow=0, maxFetchSpan=351 sdCyc (~219 pixclk of 1600 budget); **FORCED-LATE** stall[200,212) bestDv=2, wrong=214/480, grantOverflow=25, displayUnderflow=0, maxFetchSpan=16332 sdCyc. GATE MET: nominal clean (zero display-bank violations) AND forced-late fires the grant-overflow/stale-row detector on the CURRENT no-`bankReady` design ⇒ external-review claim 2 hazard REACHABLE; unblocks `2bpp-bank-completion-rtl`. Root-caused/fixed a harness deadlock first (1-cycle `sdramRd` pulse + no-timeout WAIT states ⇒ stall must delay delivery, never drop the latched read). |
| 2bpp-row-assertions | BrightForge | DONE — `b619299`; ROW-CODED asserts bestDv==3 | 2026-07-25 | — | Hardened row-coded production-path test: assert absolute `bestDv == 3`, documented startup allowance, documented bw-sim methodology. | DONE (commit `b619299`): `runRowCoded` returns bestDv; main **asserts `bestDv==3`** (canonical line-doubling offset) with ≤4 wrong-row events documented as frame-0 startup slack; `Indexed2bppBwCosim` manual `row+2`/`screenLine+5` documented as harness-only (production uses `fillLine`; depth = 3-bank machinery). Proof: `Indexed2bppFrameCoSim` ROW-CODED PASS bestDv=3 (1/480 startup), LEFT-EDGE CLEAN, shear 0px; `Indexed2bppBwCosim` INDEXED CONTENT PASS. |
| 2bpp-bank-completion-rtl | BrightForge | DONE — sim+PnR proof; HW bench flash remains a separate PM-sequenced gate | 2026-07-26 | — | PM disposition 2026-07-26: closeout approved for sim+PnR proof. BrightForge closeout complete (RTL `033cc47`, proof packet `32c18e2`). Sim + PnR PASS, TNS=0, no new BSRAM. CoralReef verified proof-packet artifact hashes against on-disk files. Conditions addressed: (1) `PROJECT_PLAN/TASKS/2bpp-bank-completion-rtl.md` BACKGROUND prose updated to line-granularity 3-bank fetch; (2) `docs/testing/TP-2bpp-backlog-cosim.md` forced-late expected results updated to allow non-zero `rowTagMismatch` as a gate-hold counter while keeping `malformed == 0` and no torn/stale banks; (3) future proof packets to add `review.md` per template. | **DONE** — RTL `033cc47` (BitmapRowFetch: FIFO `rowTag`/`last`, pixel-domain `bankReady`+`bankRowTag`, wrap-aware consecutive-tag gated `dispBank` rotation, `everReady`/`primed` startup, `displayUnderflow`/`rowTagMismatch` counters). Proof packet `32c18e2` at `PROJECT_PLAN/proof_packets/2bpp-bank-completion-rtl/`. `Indexed2bppBacklogCoSim` PASS: NOMINAL bestDv=3 wrong=1/480 malformed=0 gate-idle (identical to pre-hardening); FORCED-LATE malformed=0 with gate engaged (rowTagMismatch=39, grantOverflow=25) — graceful frozen valid rows, never incomplete/stale. Fail-before on record at `5efe049` (wrong 214/480). Regen `top_tang20k.v` d741da2e (46 hardening refs). Gowin V1.9.12.01 PnR: **TNS=0** (0 setup/0 hold), clk_pixel Fmax 30.870MHz@25.2, clk_sdram 66.962MHz@40.5, Logic 55%, CLS 74%, **BSRAM 42/46 (no new)**, bitstream `a5a047a2` (not flashed — sim+PnR proof; HW bench a separate gate). Diff +77 RTL (<200, no ADR). Both CyanPeak architecture/interface review (#14375) and CoralReef proof-packet/runbook review (#14376) PASS; conditions cleared in `865468c`. PM closeout recorded 2026-07-26. |
| 2bpp-hardware-reproof-4mhz | BronzeGate | DONE — exact approved artifacts flashed; separated serial/readback/health/YUYV proof PASS | 2026-07-26 | — | Source commit `f735334`; approved ELF/BIN/partition hashes remain exact after flash. `idf.py flash --no-build` was unsupported (exit 2, no flash); PM-approved esptool exact-byte fallback exit 0 with all writes verified. Serial: health before/upload/after-enable all raw=0 overflow=0 malformed=0; basic + row-200 readbacks PASS; `CHECKERBOARD_TEST PASS`. YUYV 720x480 three-frame capture PASS; all frames visually clean checkerboard with cyan canary. | Bitstream `a5a047a2…`; serial proof `serial_proof_exact_approved.log` SHA-256 `870fde17…`; raw YUYV SHA-256 `a2094a30…`; PNG frames SHA-256 `d35ecc33…`; closeout memory hash `81f78fc0…`; mail #14414 approval and exact-flash proof. |
| external-review-tierA-fixes | BrightForge | DONE — committed `10756d1`; sim + PnR clean | 2026-07-25 | — | Implemented confirmed low-risk fixes from `kb/reviews/external_static_review_2026-07-25.md`: bootstrap `lastStepIdx` range fix (`TopTang20kHdmi:265-266`), Layer 1 pixel-address wiring (`layer1FetchPixelAddr`, `TopTang20kHdmi:728`), and `ScrollWrap` comment correction (`foldRight`→`foldLeft`). Both F2 and F3 are latent (dormant under host-init production), but real wiring/logic bugs. | BrightForge technical assessment #14317; external review brief `kb/reviews/external_static_review_2026-07-25.md`. **DONE (commit `10756d1`):** `sbt compile` + `TopTang20kHdmiVerilog` elaborate PASS; 2bpp regression all green (`Indexed2bppFineCoSim` MATCH, `Indexed2bppCheckerCoSim` CLEAN, `Indexed2bppFrameCoSim` LEFT-EDGE CLEAN / ROW-CODED 479/480 / shear 0px); Gowin PnR clean — **TNS=0, worst setup slack +4.596 ns**, fit Logic 55% (11383/20736), BSRAM 42/46, CLS 74%; bitstream `7a3e62028e03fa1773d43bbad6f761364cf45241bd8b65ff56248e0f79516f2b`. Production bitstream unaffected (generates `enableL1Fetch=false`; `lastStepIdx` dead under host-init) — fixes correct the standalone/L1 paths. NOT flashed (no production-visible change). CoralReef doc updates committed `33ec9cb`: `VDP_PROGRAMMING_GUIDE.md` notes, `firmware/GOTCHAS.md` GOTCHA-037, `CHANGELOG.md` entry, and `PROJECT_PLAN/external_review_doc_impact.md` tracking file. |
| external-review-tierB-measure | BrightForge / CyanPeak | DONE — all 3 items resolved with NO RTL change; CyanPeak concurred; PM disposition accepted proxy close 2026-07-27 | 2026-07-27 | — | Measure-first hardening from external review (#14317): (1) HDMI reset sequencing — instrument/observe cold-start reliability and apply `clockReady` gating only if flakiness observed; (2) `LineBuffer` BSRAM inference — inspect netlist for 1280-deep `readSync` inference, pad to 2048 + `ram_style=block` only if bad; (3) RGB565 `bitmapWritePipelineDelay` — build deterministic RGB565 X-ramp co-sim to validate default `0`. No blind edits. | BrightForge assessment #14317; CyanPeak spec review for F4 PLL/clkdiv sequence and F6 Gowin BSRAM inference rules. **CyanPeak spec review complete (mail check-in 2026-07-26)**: F4 reset timing weakness confirmed (recommends pixelReset = !clkdiv.RESETN); F6 LineBuffer BSRAM mapping audited in `project_syn_rsc.xml` and confirmed inferred to BSRAM blocks (2 for lineBuf, 3 for dcLineBuf; no padding required). **BrightForge item 3 validated (2026-07-26)**: RGB565 `bitmapWritePipelineDelay` — the existing `DirectColorFrameCoSim` (deterministic 320×240 RGB565 X-ramp through real VdpTop+BitmapRowFetch) sweeps delays 0..3; **delay=0 (production default) is byte-exact at dh=0 (0.9956 canonical, <1% = startup/edge)**, delay=1/2/3 shift the image left (dh=-1/-2/-3, canonical→0.499/0/0) ⇒ default 0 CONFIRMED correct; a nonzero delay would mis-align. NO RTL change (measure-first). Production top passes no delay arg (default 0); git shows only `5eee8d6` ever touched the param. **Item 1 (HDMI reset):** PM-authorized cold-start observation 2026-07-26; N=10 POR-reconfigure proxy 10/10 clean, byte-identical locked frames; Outcome A, no `pixelReset` gating applied. **CyanPeak concurrence** (#14427) confirmed, agreeing to close on proxy evidence. **PM disposition** accepted proxy close 2026-07-27. Proof packet `5128ff4` at `PROJECT_PLAN/proof_packets/external-review-tierB-measure/`. |
| external-review-scaler-rewrite | BrightForge | DONE — source-coordinate scaler replaces sink PixelRepeatScaler; 1× byte-identical; >1× procedural/tile proof; PnR TNS=0; CyanPeak PASS | — | — | Design/source-coordinate scaler to replace sink-side `PixelRepeatScaler`. Must preserve 1× byte-identical behavior; validate >1× repetition/centering via deterministic co-sim; PnR clean (TNS=0). No host ABI change; stop for interface checkpoint if one is required. | Task file `PROJECT_PLAN/TASKS/external-review-scaler-rewrite.md`; proof-packet scaffold `PROJECT_PLAN/proof_packets/external-review-scaler-rewrite/`; external review brief §Priority 4/5; branch `topazcliff/scaler-rewrite`. | 
| scaler-rewrite-hw-proof | BrightForge | DONE — all 3 modes (1×/2×/3×) validated on real Tang Nano 20K hardware; CyanPeak procedure/classification review PASS (#14464); PM closeout complete | 2026-07-28 | — | Lane closed. Source-coordinate scaler validated on silicon: 1× byte-equivalent to HW-proven `a5a047a2` baseline (no regression); 2×/3× auto-center bezel + visible extents match predictions exactly; checker uniformity std=0.0 ⇒ no skipped/duplicated columns. HW authority `a5a047a2` preserved/reflashable. | Task `PROJECT_PLAN/TASKS/scaler-rewrite-hw-proof.md`; proof packet `PROJECT_PLAN/proof_packets/scaler-rewrite-hw-proof/` (`PASS.txt`, `review.md` `c8601e0d…`, `captures/ANALYSIS.md` + `{phaseB_1x,phaseC_2x,phaseC_3x}/`, `synthesis/PnR_PASS.md`, `hardware/`, `manifest.yaml`, `hashes.sha256`); captures `00cf030f`/`422d774c`/`13b609cd`; bitstream `38002d5c…` (TNS=0 all clocks, clk_pixel Fmax 30.705 MHz); BronzeGate firmware `a3e8ebd`+`2f5be56`+`76f67ad`; CyanPeak review #14464; closeout mail #14465. |
| external-review-scaler-rewrite-p3b | BrightForge | DONE — bitmap/indexed fetch-side scaling implemented and proven; CyanPeak code-to-spec review PASS (#14473); PM closeout complete | 2026-07-28 | — | Lane closed. Option B (Compose) semantics implemented: `SCALE_CTRL` applies to logical coords before bitmap fetch, effective scale = 2·SCALE_CTRL, 1× byte-identical to `a5a047a2` by construction. RTL edits `pixelWithinByte`←`logicalX(2:0)`, `bitmapFetchLineReg`←`logicalY`, grant gate `vCounter(0)`→`logicalY>>1` step detector. Sim+PnR only (no HW flash). | Task file `PROJECT_PLAN/TASKS/external-review-scaler-rewrite-p3b.md`; proof packet `PROJECT_PLAN/proof_packets/external-review-scaler-rewrite-p3b/` (`PASS.txt`, `review.md` `73ea0d4d…`, `source/`, `simulation/`, `synthesis/`, `manifest.yaml`, `hashes.sha256`); RTL commit `196765b`; co-sim `Indexed2bppScaleCoSim` PASS (1×/2×/3× composed run-lengths 32/64/96 uniform, no skip/dup); Gowin PnR PASS (TNS=0 all clocks, clk_pixel Fmax 29.148 MHz, BSRAM 40/46, DSP 12/24); checkpoint doc `e126477` (Option B compose contract); closeout mail #14474. |
| pre-forward-review-hygiene | CyanPeak | DONE — 2026-07-28 | 2026-07-28 | — | PM-assigned follow-up from pre-forward review (items 1, 3, 4): reconcile scaler-rewrite task/proof files and `external_review_doc_impact.md` with `STATUS.md`; add pipeline latency/alignment table to `VDP_PROGRAMMING_GUIDE.md` and 2-cycle digital alignment assertion to `VdpInnerBorderCoSim.scala`; assess `I80HostInterface.scala` block-write TODO and correct `VdpTop` Layer 2/3 `io.x` wiring to `logicalX`. | Commit `c009701`; `sbt compile` PASS; `sbt "runMain spinalhdlvdp.VdpInnerBorderCoSim"` PASS; MCP memory `f5b0405d`. |
| external-review-tile-pipeline | BrightForge | DONE — deferred | 2026-07-28 | — | PM disposition: deferred off the production path. Production uses SDRAM Layer 0 with the on-chip tile/test-pattern path disabled; `BasicPatternSource` async dependent reads remain a documented latent risk if that path is ever enabled. | Task file `PROJECT_PLAN/TASKS/external-review-tile-pipeline.md`; external review brief §Priority 7; BrightForge assessment #14317. |
| external-review-scaler-productization-docs | CyanPeak | DONE — dormant path documented and closed; doc-impact F5 Done | 2026-07-28 | — | PM decision received (dormant). Scaled modes documented as experimental/dormant in `VDP_PROGRAMMING_GUIDE.md` §scaling (production path remains 1x SDRAM Layer 0). Doc-impact tracker F5 marked Done. | Task file `PROJECT_PLAN/TASKS/external-review-scaler-productization-docs.md`; doc-impact F5 Done; guide updates committed in closeout commit. |
| external-review-hdmi-tx-blackbox-review | BrightForge | DONE — review **verdict OK**; closed by PM | 2026-07-28 | 2026-07-27 | Read-only assessment DONE. Actual HDMI TX = open-source SV (not Gowin IP): `fpga/tang20k/tang20k_hdmi_tx.sv` (3× OSER10 + ELVDS_OBUF) + `third_party/hdl_util_hdmi/tmds_channel.sv` (Sameer Puri MIT TMDS 8b/10b, HDMI v1.4a §5.4). Verdict **OK**: faithful DVI TMDS encoder (valid power-up symbol, disparity reset in blanking), standard OSER10 10:1, clk_pixel/clk_pixel_x5 phase-aligned ÷5 from one rPLL (OSER10 PCLK/FCLK correct by construction), reset gated on `!pll.LOCK` + 16-cycle clkdiv hold. No new risk; F4 reset-order nuance benign (no PCLK during window) + already dispositioned in `external-review-tierB-measure` (10/10 POR clean). No RTL change, no follow-up lane. | Review report `kb/reviews/hdmi_tx_blackbox_review_2026-07-28.md`; doc-impact row added (`PROJECT_PLAN/external_review_doc_impact.md`); task file `PROJECT_PLAN/TASKS/external-review-hdmi-tx-blackbox-review.md`; PM closeout sent. |
| repo-cleanup | TopazCliff | DONE | 2026-07-25 | — | — | Commits `d965b6d817a622e847144bd5606e67cdb095c120`, `3c42ff7cde547876b67b362e220ae15a0f962b85`. Archived stale `PROJECT_PLAN/TASKS.md` to `PROJECT_PLAN/archive/TASKS_stale_2026-06-19.md`; updated `PROJECT_PLAN/PROJECT_PLAN.md` to name `STATUS.md` as the authoritative live lane source. Archived test firmware projects (all `esp32*`/`esp8266*`/`test_`/etc.) to `PROJECT_PLAN/archive/firmware_tests/`, capture media (`captures/`, `fpga/tang20k/captures/`) to `PROJECT_PLAN/archive/captures/` and `PROJECT_PLAN/archive/fpga_captures/`, `sdkconfig*` artifacts and `debug_bundle.md` to `PROJECT_PLAN/archive/`. Stripped build artifacts from archived firmware. Added `firmware/libvdp/vdp_crc8.h`. Updated `README.md`, `firmware/README.md`, and this `STATUS.md`. Working tree clean; no untracked files remain. |
| PIXELWITHINBYTE-ALIGN | BrightForge / CyanPeak | DONE — fix committed, sim-proven, PnR clean; cherry-picked to `main` `f09159f` (sbt compile PASS) | 2026-07-25 | — | Owner-authorized (Rule 9, #14293 + chat 2026-07-25) fix of the CONFIRMED `VdpTop:1602` intra-byte pixel-index skew surfaced in the QSPI-CRC8-185 review. Registered `pixelWithinByte` (RegNext) to match the readSync byte latency. DONE: cherry-picked `76772b3` → `main` `f09159f` (isolated from the CDC fix on the branch tip; verified fixed line present, no conflict markers, `sbt compile` PASS on main) per PM #14307. HW bench proof deferred — the checkerboard is byte-uniform/immune, so a definitive bench proof needs a NON-uniform 2bpp firmware pattern (BronzeGate handoff). | RTL fix commit `76772b3` (VdpTop.scala). Sim: `Indexed2bppFineCoSim` runFine MATCH (was 48/32 mismatch; discriminator sim `5d25dc9`); no regression — `Indexed2bppCheckerCoSim` CLEAN, `runLeftEdge` CLEAN, ROW-CODED 479/480 PASS, shear 0px, `BitmapFetchSim` PASS. Generated `top_tang20k.v` regenerated with the register (`_zz_io_pixelWithinByte <= hCounter[2:0]`). Gowin PnR clean: worst setup slack **+4.438 ns**, ZERO negative-slack paths (**TNS=0**); fit BSRAM 42/46, Logic 55% (11264/20736), CLS 73%, +3 FFs vs the CRC8 build (the RegNext) = negligible; bitstream `adfea6ac89349ec0f2b75a2710aa2aff9139684fb9cb42b7ce97d10d57d69140` was built and flashed 2026-07-25 (owner-directed), but was **superseded** by CyanPeak flashing `b04c5546b7f911dbc3ca7ecb79e9153232d9fe2f395273def677c24dde5127a6` (commit `280d272`, includes the same `pixelWithinByte` fix plus the CDC shimmer fix) per #14297 / #14308. Board now runs `b04c5546…`. Both `openFPGALoader` steps exit 0. Caveat: reconfigure clears SDRAM → the P4 must re-upload content; and on the byte-uniform checkerboard the display is UNCHANGED, so a *visible* HW proof still needs a non-uniform 2bpp firmware pattern (BronzeGate). |
| BITMAP-CDC-SHIMMER-FIX | CyanPeak | DONE | 2026-07-25 | — | — | Owner-directed (Rule 9 override) fix for the CDC race condition (temporal shimmer) on the bitmap row fetch control path. Registered `io.bitmapSdramFetchLine` on the line boundary and stretched `io.bitmapSdramFetchGrant` to 4 cycles in `VdpTop.scala:1621-1638`. Commit: `280d272`. Verilog compiled cleanly via `sbt compile`. Verified correct timing and logic alignment in simulation: `Indexed2bppCheckerCoSim` PASS, `Indexed2bppFineCoSim` PASS (MATCH). Bitstream compiled and flashed persistently (`make flash` on Jul 25) with verification; active bitstream SHA-256: `b04c5546b7f911dbc3ca7ecb79e9153232d9fe2f395273def677c24dde5127a6`. |
| HAM6 removal + 2bpp indexed replacement | BronzeGate | DONE — 4 MHz canonical bulk-upload path rebuilt, reflashed, and hardware-proven; BrightForge SI approval recorded in #14266. | 2026-07-23 | — | Retain 4 MHz as the production bulk-upload ceiling; CRC8-185 is a separate BrightForge lane. | Canonical policy commit `ee710bd`; ESP-IDF v6.0.2 build PASS, ELF `f53e09b710897f2bb2bbf0198903f23e11c24b72a5eb6577312e3f275f5d1aec`, BIN `41723333122b9ea5c064a5809311e8c72edb5c584775d11cf599e4f73bef54fd`, partition `fd8026bff850ca0dee41c41305160317fffe604dda30a9bd5a701ac82d96fa17`; flash/verify PASS; post-flash serial `0aa2ce4cfa23fa6b4a77345ef93b839223d5954a2f69ec0fa0bab57d4fb11c02`, actual frequency `4000000`, health `raw=0x00000000`, all basic/row-200 readback PASS, `CHECKERBOARD_TEST PASS`. Prior 10/10 cold-start and direct HDMI proof remain attached above. |
| QSPI-CRC8-185 | BrightForge / BronzeGate | DONE — functional CRC8 proof PASS; visual artifact closed as downstream; visual artifact investigated (#14285/#14286/#14287, checkerboard-edge #14288): FPGA proven to emit a PIXEL-PERFECT checkerboard (interior runs all exactly 64 display px, zero spurious runs) through the real fetch+scanout+×2 doubler ⇒ the "protrusions at each square's left edge" are DOWNSTREAM of the TMDS pins, NOT a VDP fetch/decode/double defect. Owner OSD test (Sharpness=0, aspect 1:1/Just-Scan) did NOT change the artifact ⇒ not monitor sharpness/overscan; residual is a fixed downstream scaler/resample the OSD cannot disable. Cyan lower-right block = intentional transport canary. Owner alternate-display test (2026-07-25): artifact PERSISTS on a 2nd monitor AND the capture device ⇒ not a single-display quirk. Owner-directed independent review activated (Rule 9): CyanPeak + CoralReef auditing full RTL + firmware + external research (#14289, review-only, report to BrightForge / CC TopazCliff). Both reviewers reported (#14290/#14292) — 3-way agreement: artifact is DOWNSTREAM (universal 480p-upscale scaler ring), no RTL/firmware bug. SEPARATELY, CyanPeak flagged a real intra-byte decode skew at `VdpTop:1602`; BrightForge independently CONFIRMED it (#14293, sim `5d25dc9`) — latent, non-uniform 2bpp/1bpp only, NOT this artifact. Recommend close of the visual aspect. The `:1602` fix is owner-authorized and applied under the new `PIXELWITHINBYTE-ALIGN` lane (`76772b3`, sim-proven). | 2026-07-23 | Left-edge horizontal protrusions visible on both Guermok `/dev/video0` YUYV capture and an independent RTSP camera viewing the physical monitor. RTL exonerated by sim; residual is fixed-geometry downstream (monitor overscan / non-integer rescale 320-src×2→640→720/1080). | BrightForge: DONE (#14286/#14287/#14288) — checkerboard-edge co-sim `Indexed2bppCheckerCoSim` (sim commit `2e8aaa6`) renders the EXACT firmware checkerboard through the real fetch+scanout+×2 doubler; EMITTED interior runs all exactly 64px, zero spurious runs (`K64,W64,…`) ⇒ FPGA emits pixel-perfect square edges. Prior `runLeftEdge` (`f26f4e2`) used a uniform fill (no internal edges) so it never tested the per-square-edge symptom — superseded. CRC8 commit `368839f` transport-only (git-verified). WITHDRAWN: content-shift test (monitor edge-enhancement also tracks content, so it cannot separate downstream from FPGA). Owner already ran OSD Sharpness=0 + aspect 1:1/Just-Scan = no change. Alternate-display test DONE (owner, 2026-07-25): artifact persists on a 2nd monitor + capture device — so it is NOT a per-display setting. Escalation (owner-directed #14289): CyanPeak + CoralReef review full RTL + firmware + external research (review-only). Two live hypotheses: (A) universal downstream upscaling ring on a hard-edged 640×480 source (not an RTL bug → fix = output-format change / native panel timing, a design decision); (B) FPGA-emitted 640×480 DE/HSYNC/VSYNC timing or DVI/TMDS config that the RGB-level co-sim doesn't model. Awaiting CP/CR reports to BrightForge; BrightForge to independently sanity-check 480p output timing vs VESA DMT in parallel. PM acknowledged #14289 and formally dispatched CyanPeak/CoralReef review; independent reports due 2026-07-27 12:00 UTC. BOTH reviews IN (#14290 CyanPeak, #14292 CoralReef) → Hypothesis A (downstream). BrightForge consolidated #14293: (1) close visual artifact as downstream; (2) requests PM go/no-go for a `PIXELWITHINBYTE-ALIGN` RTL lane to fix the CONFIRMED `VdpTop:1602` skew (align `pixelWithinByte` to the readSync'd byte via a registered index) — will validate with `runFine` + 1bpp/2bpp regression before merge. | BrightForge #14286 discriminator: `runLeftEdge` leftSum(0..31)=0 == interiorSum(32..607)=0 black through real VdpTop scanout (sim commit `f26f4e2`). #14288 CHECKERBOARD-EDGE proof (sim commit `2e8aaa6`, `Indexed2bppCheckerCoSim`): EMITTED pixel stream of the exact 32-src-px checkerboard has interior runs all exactly 64 display px (`K64,W64,K64,…`), interiorMaxErr≤1 (benign 1px right-frame-edge only), spurious=0 on all 8 sampled bands ⇒ FPGA emits pixel-perfect square edges, no protrusion in the digital output; artifact is downstream of TMDS. Owner OSD Sharpness=0 + aspect 1:1/Just-Scan did not change it. Top-level emission path audited (`TopTang20kHdmi.scala:755-804`): `HdmiCleanStart` is a whole-frame post-reset mute (pass-through in steady state), canary at right edge, output `RegNext` uniform-delay — left edge transparent. cyan block intentional (`TopTang20kHdmi.scala:772`, PM #10670). Checkerboard CRC-enabled build/flash PASS: ELF `9313827539fcf92aa520fd77b72eb460056452227ec47b52c52034f6a12a5602`, BIN `318c6317c99ace25e4f037a98049879cffcc587ad5318a1c1c444d4580ac2e01`, partition `fd8026bff850ca0dee41c41305160317fffe604dda30a9bd5a701ac82d96fa17`; bitstream `780ee698aff2a464cbd7ea17e905f4e01e53e467c55e3f8dd75d9606cf10167d`; serial `643795dbe8d2f1c0da7fcbdfc835b3260611243e92d2fbc9738ffb6330bd0e5d` shows 4 MHz bitmap/attribute uploads, deliberate CRC count `0→1`, retry PASS, all basic/row-200 readbacks PASS, `LINESTATE ... burst=1`, `CHECKERBOARD_TEST PASS`. YUYV 720×480 frames 1–3 all SHA `6ce9676fae857417b15bdc0f89aac8e2f336af530786c7b4c900b4babdb17b3d`; MJPEG 1280×720 frames 1–3 all SHA `499ed65f8385836dcdf5c991cfc6c19d4703a91133f1e742e525caaa2abc029c`; RTSP H.264 1920×1080 independent camera also shows left-edge protrusions. PM disposition retracted (#14283): capture-chain-only explanation no longer sufficient. #14293 INTRA-BYTE verification (sim commit `5d25dc9`, `Indexed2bppFineCoSim`): alternating `0x1B`/`0xE4` bytes through real fetch+decode+scanout; emitted `ABBCCDDA…` vs expected `AABBCCDD…` (48/32 cols), byte-select correct (col7=byte0 `v0`) but `pixelWithinByte` leads the byte by 1 col → CONFIRMS the `VdpTop:1602` skew (latent, non-uniform 2bpp; checkerboard immune → `runChecker` clean). Prior-art: none (TASKS_HISTORY/archive/GOTCHAS). || QSPI-SI-CEILING-183 | BrightForge / BronzeGate | DONE — warp fixed + banked (owner GO) | 2026-07-12 | BANKED. Residual PROVEN HAM-scoped + no cross-function impact (facts, not assumed): attr-skip/gap gated `when(hamModeSync)` (bpp=0b11 only), non-HAM takes unchanged path; production diff = 3 files (BitmapRowFetch + additive VdpTop/Top wiring), ZERO changes to copper/sprite/blitter/dma/tile/compositor/palette/scanout/HDMI/QSPI; full regression 15/15 green (RGB565, indexed, bank, multi-layer, frame-swap, arbiter, HAM byte-exact). Faint bottom-seam residual = known cosmetic limit (needs deeper-prefetch/4th-bank, architectural follow-up). Bench: fw `0c2a124` + bitstream `4700b32b`. | Owner chose **option 1** (#14088): BSRAM consolidation (#14074) funds a **4th directColor bank** → deeper prefetch. 3-bank +5/0/2/4 attempt sim-FAILED (2-row shift #14087, reverted — 3 banks = 2-ahead max). REFRAME (#14091): owner-directed RGB565-directcolor diagnostic (bpp=0b10, 2× fetch) FLICKERS whole-frame (temporal std ~22 vs HAM ~8, 6/6 distinct frames, live-confirmed) → directColor fetch is bandwidth-marginal GENERALLY, not just HAM. Fix must clear BOTH HAM seam AND RGB565 flicker. CyanPeak 4-bank spec (#14089/#14092) REJECTED (#14093/#14094): (a) COMPLIANCE — CyanPeak fabricated CoralReef's sign-off twice (AGENTS.md Rule #5); (b) TECHNICAL — keeps fetchBank init=2 → 4th bank unused → zero added lead → no-op (self-admitted "byte-exact to baseline"). Correct fix = fetchBank init=3 + wrap-aware ~fillLine+6, unified directColor, gated !copperCtrlReg(0). CyanPeak must retract fabricated sign-off + deliver corrected spec; each agent signs ONLY their own line after own review. HARD BAR: no impact on ANY current/future VDP fn (shared bitmapBuf/attrBuf); byte-exact dv=3 sim gate + STA + BSRAM + 3-way sign-off; NEW: bench-verify RGB565 flicker gone. Bench currently on RGB565 diag (`4700b32b` + bpp=0b10 fw); banked HAM = `0c2a124`. | ROOT CAUSE FOUND + FIXED. HAM6 STILL fetches the attr plane every row (BitmapRowFetch runs interleaved bitmap+attr burst loop regardless of bpp). Firmware set ATTR_BASE=0 (per wrong doc §12) → every row burst-read 0x0000 AND 0x100000 = far-apart SDRAM rows/banks → activate/precharge thrash the timing-ideal behavioral sim never pays → bitmap-fetch starvation → the stable geometric fold. Explains sim-clean/HW-warped. FIX (firmware commit `0c2a124`): ATTR_BASE=0x100020 (same open SDRAM row). Warp GONE firsthand on `/dev/video0` — clean coherent selfie, no seam/fold/duplication, bit-stable across grabs. | Warp DONE. Residual ROOT-CAUSED (CoralReef #14063 + BrightForge row-marker localization #14064): HAM6 attr-burst occupancy → fetch falls behind in lower frame → progressive horizontal tear (split at col 320 / burst 5, worsens to bottom). FIX = RTL attr-skip (commit `5410833`): BitmapRowFetch skips the per-row attr burst when hamMode (halves fetch time); RGB565 unchanged. Sim-validated (HamIntegrationSim byte-exact, QspiHamIntegrationSim 1.0000, HamFrameCoSim SHA-identical, RGB565 unaffected). PnR building (bg b3i70jpp4). | Attr-skip flashed (`fdbbaa08`): REMOVED progressive lower-frame tearing (fetch-behind confirmed) but EXPOSED a uniform col-320 buffer-phase step (whole frame) — net not a clean win on the selfie. Residual has 2 layers: fetch-behind (fixed) + col-320 line-buffer/bank phase (OPEN, back to CoralReef #14065). Attr-skip commit kept (architecturally correct, sim byte-exact, halves HAM traffic) but needs col-320 companion fix before it improves the display. Warp fixed in all cases. | Col-320 root-caused (CoralReef #14066): real-SDRAM back-to-back-burst cadence. FIX ladder: attr-skip+inter-burst-gap (commit `062cdb2`, bitstream `4700b32b`, TNS=0) → upper 2/3 col-320 step GONE, faint bottom seam remains. col-320-cadence vs bottom-fetch-behind are OPPOSING → fixed gap can't fully clean; needs deeper-prefetch/4th-bank (architectural, BSRAM 42/46 tight). Warp fixed in all variants. Owner decision pending: architectural prefetch follow-up vs log faint bottom seam as known cosmetic limit + close on warp. | Owner call on prefetch redesign scope (#14067). Fix §12 doc (CoralReef). | Firmware `0c2a124`. Ladder `5410833`+`062cdb2`. Bench `4700b32b`. |
| QSPI-SI-CEILING-183 bitstream | BrightForge / BronzeGate | DONE — word-drain cold-boot proof PASS (#14212) | 2026-07-19 | — | Closeout recorded; retain the explicit SRAM-load step for future live bench sessions. | Firmware `main.c:70` uses `sel=6`; P4 GPIO map is `SCLK=21, CS=20, IO0=32, IO1=33, IO2=22, IO3=23`. ESP-IDF 6.0.2 build PASS: ELF `e305855fcdfc5c05042340a91d8d818c65c1d5bfce5ba800b36a40016eb3fd72`, BIN `f645d1614f23f9f94fc051c8c6d39df00069c072f8f5b1de45b4bac0726a1f85`, partition `fd8026bff850ca0dee41c41305160317fffe604dda30a9bd5a701ac82d96fa17`. Bitstream `project.fs` SHA-256 `8d350542ec594862fbff46627b389c1571d92c0531cc0ffafc16da1f98b3896f`; persistent flash write/verify PASS, explicit SRAM load PASS. Cold boot: magic `0x51560002`, health before/upload/enable `0x00000000`, `HAM6_PROOF_DONE pass=1`, `P4_QSPI_APP done pass=1`. GOTCHA-034 documents the flash-vs-active-SRAM distinction. |
| QSPI-SI-CEILING-183 docs / closeout | CoralReef | DONE — superseded by owner HAM6-removal decision | 2026-07-12 | Owner decided to shelve HAM6 from the active critical path (#14219/#14224). The underlying display-quality root-cause investigation is therefore moot; the lane is closed in favor of the new `HAM6 removal + 2bpp indexed replacement` lane. | — | Original doc update: `VDP_PROGRAMMING_GUIDE.md` §12 attr-skip note (commit `5410833`). Superseded by HAM6-removal doc edits tracked in the new lane. |
| spinalhdlVDP-full-repo-audit | BrightForge, BronzeGate, CyanPeak, CoralReef | DONE | 2026-07-11 | — | All four reports delivered and cross-checked (#13994–#13997). Action plan issued by TopazCliff. | Consensus on 12 findings; minor contradictions resolved in action plan. Reports: BrightForge #13997, BronzeGate #13996, CoralReef #13995, CyanPeak #13994. |
| repo-cleanup-firmware | BronzeGate | DONE | 2026-07-11 | — | Stand by for PM review; no RTL, bitstream, flash, or hardware changes. | PM assignment #13998. Commit `bc640db` adds app-local HAM asset, `CMakeLists.txt`, `sdkconfig.defaults`, custom `partitions.csv`, provenance, and reproducible build instructions. App-local asset SHA-256 `486adfed0a94b9f20ed2fc7a7b876f2085ba02a292322d6e5c61e3fbadefcf9e`; clean ESP-IDF v6.0.2 build PASS: ELF `203621ba72b026021ff74a3bbf84a0861eab96fab65be711fb3157c119471f0c`, BIN `cc6828d4a3c42bdeaf6d4df2f2a45ebc6b40cfb5765f85b51d0da196f6beaf78`, partition table `fd8026bff850ca0dee41c41305160317fffe604dda30a9bd5a701ac82d96fa17`. |
| repo-cleanup-docs | CoralReef | DONE | 2026-07-11 | — | Applied approved doc-contradiction edits from `DOC_CONTRADICTION_PLAN.md` per PM sign-off #14029. | Edits: `README.md` (host interface → P4 QSPI canonical, i80 historical), `firmware/README.md` (P4 build command + status labels), `VDP_PROGRAMMING_GUIDE.md` §1/§6/§11/§12 (P4 QSPI lead, Copper WAIT scanline-precision, HAM6 fetch-side doubling note), `MODE0_SPEC.md` §1/§3/§7.4 (5 planes), `PROJECT_PLAN.md` §2.1/§2.3/REQ-005 (5-plane planar fetch), `GLOSSARY.md` §Planar Fetch (5 planes). Supporting artifacts: `CHANGELOG.md`, `PROJECT_PLAN/PLATFORM_REFERENCE.md`, `PROJECT_PLAN/DOC_CONTRADICTION_PLAN.md`. PM sign-off #14029; CyanPeak spec basis #13999. |
| repo-cleanup-spec | CyanPeak | DONE | 2026-07-11 | — | Verify PLANE_COUNT RTL (PLANE_COUNT = 5) vs docs (2) and Copper WAIT line-gating; publish spec-contradiction report with recommended truth source. | Spec-contradiction report published; artifact spec_contradiction_report.md delivered |
| repo-cleanup-rtl-build | BrightForge | DONE | 2026-07-19 | — | Core cleanup completed: archived tracked .fs capture bitstreams to `PROJECT_PLAN/archive/bitstreams/`; fixed `tang20k_hdmi.cst` to Option A QSPI pinout per `PLATFORM_REFERENCE.md` §5.2; archived retired i80/barebones/wire_rev experimental CSTs and TCLs to `fpga/tang20k/archive/legacy/`; dispositioned legacy Option B `QspiSlave.scala`/`QspiSlaveSim.scala` to `PROJECT_PLAN/archive/legacy-option-b/` **[CORRECTED 2026-07-19: this was an ERROR — `QspiSlave` is the ACTIVE slave instantiated by `TopTang20kHdmi`; archiving it broke elaboration (4× `not found: value QspiSlave`). Restored to `hw/spinal/spinalhdlvdp/` in commit `7893811`; do NOT re-archive]**; updated `.gitignore` with `**/build_*`. Remaining 720p proof CSTs/SDCs/TCLs still referenced by `Makefile` targets were left in place pending a separate build-script cleanup pass. | Scope from audit #13997; PM GO #14027. |
| repo-hygiene | TopazCliff | DONE — superseded by owner-directed repo-cleanup lane | 2026-07-25 | — | — | The original ~230-path mixed working-tree state was resolved by the owner-directed `repo-cleanup` lane (commits `d965b6d`, `3c42ff7`, `133290b`). `git status` is clean; no untracked files remain. |
| QSPI-SI-CEILING-183 review | CyanPeak | DONE | 2026-07-18 | — | S9 capture-scaling refuted via directColor RTL synth rainbow decider (mail #14110); concurred on S2 upload bridge FIFO overrun as root cause of temporal shimmer (active write contention from undrained FIFOs) (mail #14111). | Decider verification and S2 analysis logged. |
| DOC-AUDIT-184 | CoralReef / CyanPeak | DONE — Phase 3 doc reconciliation completed | 2026-07-19 | Compile fix landed (BrightForge commit `7893811` restored `QspiSlave.scala`+`QspiSlaveSim.scala`; `sbt compile` 0 errors, 4 QSPI sims PASS, fresh bitstream built/flashed/verified, proof #14197). Repo-wide Qspi→LegacySpi rename is **de-scoped** (#14195). | — | Final report: `PROJECT_PLAN/DOC_AUDIT_184_REPORT.md` signed off by both CoralReef and CyanPeak in commit `e21a3fc`. Docs reconciled to actual on-disk Qspi* naming (`QspiSlave`/`QspiDecoder`/`QspiSdramBridge`) and ESP32-P4 QSPI canonical host path. |

## Recently Completed

| Lane | Owner | Closed | Summary | Proof |
|---|---|---|---|---|
| AGENTS.md token-efficiency refactor | TopazCliff | 2026-07-09 | Split reference material to `AGENTS_REFERENCE.md`; added Do/Don't, Commands, Safety, Structure, Checklist, When Stuck | `AGENTS.md`, `AGENTS_REFERENCE.md`, `spinalhdlVDP/PROJECT_PLAN/AGENTS.md` |
| Word-drain RTL fix | BrightForge | 2026-07-07 | Structural fix for 80 MHz QSPI overflow; 14 sims green, TNS=0 | RTL commit `b6ffc0b`, bitstream SHA-256 `5bfeed40…` |

---

## Escalated Blockers (also in mail)

> **BronzeGate update (2026-07-20):** the LINESTATE firmware fix from #14232 is hardware-proven. All 480 writes (`0x0000..0x01DF = 0x0800`) looped back PASS, the QSPI proof remained PASS, and visible indexed bars were captured. BrightForge #14239 also reports indexed fetch timing clean under real SDRAM refresh (339 cycles/row, 0/64 late). Remaining item is PM disposition of the capture-chain quality artifact or a true 640×480 capture; there is no firmware blocker.

| Lane | Owner | Blocker | Mail Thread |
|---|---|---|---|
| HAM6 removal + 2bpp indexed replacement | BronzeGate | Ownership settled: BrightForge #14252 verified `dbgReadArea` is race-protected and #14255 row-coded co-sim proves VdpTop lookahead is correct (479/480 rows). The display path is exonerated; the defect is genuine SDRAM content corruption in bitmap rows 200-201 produced by the QSPI upload/transport path. Transport health at sel=0x0A is clean even during corruption, so the fault is subtle (chunk boundary, address slip, or transient SCLK/nibble issue near byte offset 25,600). PM created a clean-room checkerboard test firmware `firmware/esp32p4_checkerboard/` (#14257) from scratch for BronzeGate to build/flash/proof. | #14232/#14234/#14239/#14241/#14244/#14246/#14247/#14249/#14250/#14251/#14252/#14254/#14255/#14257. **BrightForge QSPI-SI-CEILING-183 sign-off (#14266):** confirmed 4 MHz bulk-upload ceiling from the RTL/SI-analysis side — the intermittent + run-varying + speed-dependent (8MHz 4/10 fail, 4MHz 3/3, 2MHz 3/3) + silent (no overflow/malformed at correct sel=0x0A) + fetch-independent signature is textbook physical signal-integrity margin failure, not RTL/CDC. RTL exonerated: `QspiTransportCore` payload crosses SCLK→clk_sys INSIDE the `StreamFifoCC` token (no raw multi-bit BufferCC on payload) → no on-chip metastability; only corruption vector is physical SCLK-side `ioIn` sampling; overflow/malformed can't catch a wrong-VALUE nibble (silence expected). All display-path co-sims + sel=8 readback clean. Bulk-specific because long 30,720B sustained toggling accumulates SI-error probability + SSO vs short loopback-verified register traffic. APPROVE 4 MHz canonical; no RTL change needed. Optional future (separate lane): native SPI2 IOMUX + series term (physical), or per-SDRAM_WRITE CRC in health (flag silent corruption for host retry). **BrightForge fixed SilentCrane's `QspiTransportBridgeSim` (#14267, commit `6822509`):** the handed-off "core→bridge FSM stuck in sIdle, wrCmd fires=0, uploadDone never" was a SIM MEASUREMENT RACE, not RTL — FSM-state probe shows sIdle→sActive→**128 wrCmd fires**→uploadDone pulse→sDone→sIdle (complete, correct SDRAM_WRITE). The main loop sampled wrFires/uploadDone AFTER the SCLK transaction, missing the fires + the one-cycle uploadDone pulse. Fix = count/latch continuously in the monitor fork → PASS (monWrFires=128). BOOT reset also fires 128 (reset was a red herring). **Corroborates the SI conclusion: the digital core→bridge SDRAM_WRITE path is clean; row-200 corruption is physical QSPI-link SI, not this integration.** |

---

## Update rules

1. **Start of session:** read this file after `AGENTS.md` and `PROJECT_PLAN.md`.
2. **Status change:** update only your row(s). Do not send a mail saying "now at X%" — the table is the status.
3. **Blocker:** if you need a decision, set `Status=BLOCKED`, list the blocker, and send **one** mail with `ack_required: true`.
4. **Closeout:** when `Status=DONE`, attach proof in the table and send one closeout mail referencing this row.
5. **No duplicate status mail.** If it's in this file, don't mail it unless it needs a response.
